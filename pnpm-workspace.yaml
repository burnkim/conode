packages:
  - "apps/*"
  - "packages/*"

# esbuild ships its native binary via an optional platform package; approving its
# install script silences pnpm's ignored-builds warning. Vite depends on it.
allowBuilds:
  esbuild: true
