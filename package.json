{
  "name": "conode",
  "version": "0.0.0",
  "private": true,
  "description": "conode — 실시간 AI 비주얼 퍼포먼스 엔진 (monorepo)",
  "type": "module",
  "packageManager": "pnpm@11.5.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @conode/studio dev",
    "build": "pnpm --filter @conode/studio build",
    "check": "pnpm --filter @conode/studio check",
    "test": "pnpm --filter @conode/studio test",
    "schema:generate": "pnpm --filter @conode/schema generate",
    "tauri": "pnpm --filter @conode/studio tauri"
  }
}
